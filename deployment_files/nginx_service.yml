apiVersion: v1
kind: Service
metadata:
  name: nginx
  namespace: ipchecker-pre-release
spec:
  selector:
    app: nginx
  ports:
    - protocol: TCP
      port: 443
      targetPort: 443
      nodePort: 32444  # Specify the NodePort or let Kubernetes assign one
  type: NodePort
